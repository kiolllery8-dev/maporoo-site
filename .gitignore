node_modules/
.next/
out/
.env
.env*.local
*.log
.DS_Store

# SQLite 資料庫與後台上傳檔 —— 這是營運資料，不進版控。
# 備份請直接備 data/ 這個目錄。
/data/
/public/uploads/

# analysis artifacts (not part of the site)
.aesop-analysis/

# large source video at repo root — canonical copy lives in public/videos/hero.mp4
/MAPOROO*.mp4

# firecrawl scrape cache
.firecrawl/

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Claude Code local state
.claude/settings.local.json
.claude/settings.local.json.tmp.*
.claude/worktrees/

# rm -rf .next 遇到被鎖住的檔案時，msys 會把它改名丟在根目錄
.msys*
